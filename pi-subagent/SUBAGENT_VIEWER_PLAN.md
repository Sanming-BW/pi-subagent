# Subagent viewer implementation plan (historical)

> This document is an outdated planning note kept for reference.
> It is no longer the source of truth for the extension behavior.
>
> For the current single-only subagent API and viewer behavior, see:
> [`REFERENCE.md`](../REFERENCE.md)

## Status

The extension is now single-only on the execution side, and the viewer should be understood in that context.

Use this file only as historical context for how the viewer evolved.

## Historical notes

The remaining sections from the original plan discussed earlier viewer ideas, including parallel call handling and future nested exploration. Those ideas are preserved only as design history and should not be treated as current requirements.

If you are implementing or changing the extension today, follow `REFERENCE.md` and the code in the repository instead of this plan.
